.local/*
*.log
*.bin
vendor/
output/*
*.test
/secure-rvas

CLAUDE.md
AGENTS.md
docs/*
skills-lock.json
.claude/*
.agents/*
*.pyc
.DS_Store

# VAT-derived annotation tables: the variant list alone is Controlled Tier (it reveals which
# alleles exist in the AoU callset). The extraction code is public; its output never is.
*_annotation.tsv

/data/
